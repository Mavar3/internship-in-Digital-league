<%= form_with(model: admin_welcome, local: true) do |form| %>
  <% if admin_welcome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_welcome.errors.count, "error") %> prohibited this admin_welcome from being saved:</h2>

      <ul>
        <% admin_welcome.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
